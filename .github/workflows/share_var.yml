name: SHARE_VAAR_GITHUB_ENV
on: push
jobs:
  share:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RESULT=hello" >> "${GITHUB_ENV}" # GITHUB_ENVへ書き出し Key=Value 形式
      - run: echo "${RESULT}"                       # 通常の環境変数として参照。RESULTはグローバル変数扱い 
